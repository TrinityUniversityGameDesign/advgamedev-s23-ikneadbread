using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class breadPointer : MonoBehaviour
{
    //public Transform[] targets;
    //public float speed = 5f;
    //public float arrivalDistance = 2.0f;

    //private int currentTargetIndex = 0;
    //private bool isMoving = true;

    //private void Update()
    //{
    //    if (isMoving)
    //    {
    //        MoveToTarget();
    //    }
    //}

    //private void MoveToTarget()
    //{
    //    Vector3 direction = (targets[currentTargetIndex].position - transform.position).normalized;
    //    transform.position += direction * speed * Time.deltaTime;

    //    //if (Vector3.Distance(transform.position, targets[currentTargetIndex].position) < arrivalDistance)
    //    {
    //        if (currentTargetIndex < targets.Length - 1)
    //        {
    //            currentTargetIndex++;
    //        }
    //        else
    //        {
    //            isMoving = false;
    //        }
    //    }
    //}
}
